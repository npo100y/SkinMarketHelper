<UserControl x:Class="SkinMarketHelper.Views.AdminPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="900">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#FF2D2D2D"
                CornerRadius="5"
                Padding="10"
                Margin="0,0,0,10">
            <TextBlock Text="Панель администратора"
                       Foreground="White"
                       FontSize="16"
                       FontWeight="Bold"/>
        </Border>

        <Grid Grid.Row="1">
            
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <!-- Пользователи -->
            <Border Grid.Column="0"
                    Background="#FF252525"
                    CornerRadius="5"
                    Padding="10"
                    Margin="0,0,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <DockPanel Grid.Row="0" Margin="0,0,0,5">
                        <TextBlock Text="Пользователи"
                                   Foreground="White"
                                   FontWeight="Bold"
                                   DockPanel.Dock="Left"/>

                        <Button Content="Обновить"
                                Command="{Binding RefreshCommand}"
                                Width="90"
                                Height="24"
                                DockPanel.Dock="Right"/>
                    </DockPanel>

                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding Users}"
                              SelectedItem="{Binding SelectedUser}"
                              AutoGenerateColumns="False"
                              HeadersVisibility="Column"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              IsReadOnly="True"
                              Background="#FF252525"
                              Foreground="White"
                              RowBackground="#FF333333"
                              AlternatingRowBackground="#FF3B3B3B"
                              GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="#FF444444">

                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#FF3A3A3A"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID"
                                                Binding="{Binding UserId}"
                                                Width="60"/>
                            <DataGridTextColumn Header="Логин"
                                                Binding="{Binding Username}"
                                                Width="*"/>
                            <DataGridTextColumn Header="Активные лоты"
                                                Binding="{Binding ActiveListingsCount}"
                                                Width="110"/>
                            <DataGridTextColumn Header="Роль"
                                                Binding="{Binding Role}"
                                                Width="80"/>
                            <DataGridTextColumn Header="Баланс"
                                                Binding="{Binding Balance, StringFormat={}{0:F2} ₽}"
                                                Width="110"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Grid.Row="2"
                                Orientation="Horizontal"
                                Margin="0,5,0,0">
                        <TextBlock Text="Новая роль:"
                                   Foreground="White"
                                   VerticalAlignment="Center"
                                   Margin="0,0,5,0"/>
                        <ComboBox ItemsSource="{Binding AvailableRoles}"
                                  SelectedItem="{Binding SelectedUserRole}"
                                  Width="100"
                                  Height="24"
                                  Margin="0,0,5,0"/>
                        <Button Content="Сохранить"
                                Command="{Binding SaveUserRoleCommand}"
                                Width="90"
                                Height="24"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Лоты -->
            <Border Grid.Column="1"
                    Background="#FF252525"
                    CornerRadius="5"
                    Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0"
                               Text="Лоты на маркете"
                               Foreground="White"
                               FontWeight="Bold"
                               Margin="0,0,0,5"/>

                    <DataGrid Grid.Row="1"
                              ItemsSource="{Binding Listings}"
                              SelectedItem="{Binding SelectedListing}"
                              AutoGenerateColumns="False"
                              HeadersVisibility="Column"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              IsReadOnly="True"
                              Background="#FF252525"
                              Foreground="White"
                              RowBackground="#FF333333"
                              AlternatingRowBackground="#FF3B3B3B"
                              GridLinesVisibility="Horizontal"
                              HorizontalGridLinesBrush="#FF444444">

                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="#FF3A3A3A"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>

                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID"
                                                Binding="{Binding MarketListingId}"
                                                Width="60"/>
                            <DataGridTextColumn Header="Предмет"
                                                Binding="{Binding InventoryItem.Item.Name}"
                                                Width="*"/>
                            <DataGridTextColumn Header="Игра"
                                                Binding="{Binding InventoryItem.Item.Game.Name}"
                                                Width="120"/>
                            <DataGridTextColumn Header="Продавец"
                                                Binding="{Binding Seller.Username}"
                                                Width="120"/>
                            <DataGridTextColumn Header="Покупатель"
                                                Binding="{Binding Buyer.Username}"
                                                Width="120"/>
                            <DataGridTextColumn Header="Цена"
                                                Binding="{Binding Price, StringFormat={}{0:F2} ₽}"
                                                Width="100"/>
                            <DataGridTextColumn Header="Статус"
                                                Binding="{Binding Status}"
                                                Width="90"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <StackPanel Grid.Row="2"
                                Orientation="Horizontal"
                                Margin="0,5,0,0">
                        <Button Content="Отменить лот"
                                Command="{Binding CancelListingCommand}"
                                Width="130"
                                Height="26"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
            Background="#FF303030"
            CornerRadius="3"
            Padding="5"
            Margin="0,0,5,0">
                <TextBlock Text="{Binding StatusMessage}"
                   Foreground="White"/>
            </Border>

            <StackPanel Grid.Column="1"
                Orientation="Horizontal"
                HorizontalAlignment="Right">

                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <Button Content="Сформировать отчёт по площадке"
                    Command="{Binding ExportAdminReportCommand}"
                    Width="200" 
                    Height="26"/>
                </StackPanel>
            </StackPanel>
        </Grid>

    </Grid>
</UserControl>
